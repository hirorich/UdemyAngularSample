import { OnResizeDirective } from './on-resize.directive';

describe('OnResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new OnResizeDirective();
    expect(directive).toBeTruthy();
  });
});
